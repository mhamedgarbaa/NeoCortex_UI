"""Ontology TBox extraction pipeline.

A multi-agent LangGraph pipeline that consumes preprocessed Markdown and
emits an OWL TBox (classes, data properties, object properties — no ABox).
See PLAN.md at the project root for the full architecture specification.
"""
